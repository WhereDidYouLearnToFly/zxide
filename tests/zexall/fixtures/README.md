Place `zexdoc.com` and `zexall.com` here to enable the CPU correctness gate
(`python tests/zexall/run_zexall.py`, or `pytest tests/zexall/`).

These are third-party CP/M binaries, not committed to this repo (see
`.gitignore` in this directory) -- source them yourself from a copy you
trust, and drop the two files directly in this directory.
